# TOK V04

Versione V04 di TOK.

Questa versione corregge l'errore `Cannot read properties of null (reading 'value')` causato dal nuovo campo codice stanza: il motore ora accetta il codice passato dalla nuova UI e mantiene compatibilità con il vecchio input.

## GitHub Pages
Caricare il contenuto di questa cartella nella root del repository e pubblicare `main` / root.
